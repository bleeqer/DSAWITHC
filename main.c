#include <stdio.h>
#include <string.h>

char arr[51], answer[51];
int len;

int main()
{
    scanf("%s", arr);
    len = (int) strlen(arr);

    for (int i = 0; i < len; i++)
    {
        answer[i] = 'a' + 26;
    }

    for (int i = 0; i < len - )
}